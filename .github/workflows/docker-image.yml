name: Docker Image CD

on:
  push:
    branches: [ "master" ]
    
jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout codebase
      uses: actions/checkout@v4
      
    - name: Build the Docker image
      run: docker build . --tag waynemh1216/continuous_php_image
